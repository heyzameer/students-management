<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHOPPI - Student Details</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <style>
        body {
            background: linear-gradient(to right, #e0e7ff, #dbeafe);
            min-height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
            font-family: 'Inter', sans-serif;
        }
        .student-container {
            background: white;
            padding: 2.5rem;
            border-radius: 1rem;
            box-shadow: 0 4px 20px rgba(0, 0, 0, 0.1);
            width: 100%;
            max-width: 800px;
            margin: 1rem;
        }
        .btn {
            transition: background-color 0.3s ease;
        }
        .add-btn:hover {
            background-color: #15803d;
        }
        .edit-btn:hover {
            background-color: #4338ca;
        }
        .delete-btn:hover {
            background-color: #b91c1c;
        }
        .logout-btn:hover {
            background-color: #6b7280;
        }
        table {
            width: 100%;
            border-collapse: collapse;
        }
        th, td {
            padding: 1rem;
            text-align: left;
            border-bottom: 1px solid #e5e7eb;
        }
        th {
            background-color: #f9fafb;
            font-weight: bold;
        }
        @media (max-width: 640px) {
            .student-container {
                padding: 1.5rem;
            }
            table {
                font-size: 0.875rem;
            }
            th, td {
                padding: 0.75rem;
            }
        }
    </style>
</head>
<body>
    <div class="student-container">
        <div class="flex justify-between items-center mb-6">
            <h2 class="text-2xl font-bold text-gray-800">Student Details</h2>
            <a href="/logout"><button id="logout" type="button" class="logout-btn py-2 px-4 bg-gray-600 text-white font-medium rounded-md hover:bg-gray-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500">Logout</button></a>
        </div>
        <div class="mb-6">
            <button id="add-new-student" type="button" class="add-btn w-full sm:w-auto py-2 px-4 bg-green-600 text-white font-medium rounded-md hover:bg-green-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-green-500">Add New Student</button>
        </div>
        <div class="overflow-x-auto">
            <table>
                <thead>
                    <tr>
                        <th>No.</th>
                        <th>Name</th>
                        <th>Class</th>
                        <th>Phone</th>
                        <th>Actions</th>
                    </tr>
                </thead>
                <tbody id="table-body">
                    <!-- Student data will be populated here -->
                </tbody>
            </table>
        </div>
    </div>
    <script src="/js/home.js"></script>
</body>
</html>